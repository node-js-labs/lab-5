<%- include('./partials/header', { title, isAuthenticated, currentUser }) %>

<h1 class="mb-4"><%= title %></h1>

<p><strong>Автор:</strong> <%= book.author %></p>

<% if (book.keywords && book.keywords.length > 0) { %>
  <p><strong>Ключові слова:</strong> <%= book.keywords.join(', ') %></p>
<% } %>

<a href="/books" class="btn btn-secondary mt-3">Повернутися до списку книг</a>

<%- include('./partials/footer') %>
